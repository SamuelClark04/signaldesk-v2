# Dependencies
node_modules/

# Secrets (keep .env.example tracked)
.env
.env.*
!.env.example

# Logs and OS noise
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Ledger state (paper trade history; loaded into the ledger on start)
server/data/
